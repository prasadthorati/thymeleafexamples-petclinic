# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:
    inputs:
      trigger:
        description: 'Trigger event'     
        required: true
        default: 'manual'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  Trigger:
    name: ${{ github.event.inputs.trigger }}
    
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Show trigger source
        run: |
            echo "Trigger source: ${{ github.event.inputs.trigger }}"   
#```
#I can run this manual at actions tab or my python-based REST API (PyGithub) to start my nightly. I do this in python and in a separate repository (for my organization) to handle not thousand workflow files.   
